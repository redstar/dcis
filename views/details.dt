- import dcis.state;
doctype html
head
  title D Continuous Integration Server
  style.
    body: { font-family: Sans-Serif; }
    h2: { font-size: 150%; }
    h1: { font-size: 300%; }
body
  h1 D Continuous Integration Server
  p Lightweight CI written in D
  h2 Details for #{cirun.title}
  p Author: #{cirun.author}
  p Committer: #{cirun.committer}
  p commit: #{cirun.commitSha}
  p Log:
  a(href="/") Back
